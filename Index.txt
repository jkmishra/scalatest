// List operations in sample
var (ids, indexes) = 
List("a", "b", "c").zipWithIndex.unzip
ids: List[String] = List(a, b, c)
indexs: List[Int] = List(0, 1, 2)

for(i<- 0 to 1 )
for(i<- 0 until 1 )

indexs.reduce((c1,c2)=>c1+c2)
